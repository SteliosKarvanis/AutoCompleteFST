# AutoCompleteFST

- To build, execute:
```
mkdir build
cd build
cmake ..
make
```

- To run, execute:
```
cd bin
./autocomplete
```

- To run tests, use the `test.ipynb` notebook(move it to the `bin` folder first)